services:
  api:
    container_name: evolution_api
    build: .
    restart: always
    ports:
      - "${API_PORT}:${API_PORT}"
    volumes:
      - evolution_instances:/evolution/instances
    networks:
      - evolution-net
    env_file:
      - .env
    expose:
      - "${API_PORT}"

volumes:
  evolution_instances:


networks:
  evolution-net:
    name: evolution-net
    driver: bridge
